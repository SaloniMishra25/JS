// Q3. Create new array with uppercase of words

let words = ["saloni", "javascript", "react"];

let upperWords = words.map((word) => word.toUpperCase());

console.log(upperWords); // ["SALONI", "JAVASCRIPT", "REACT"]
