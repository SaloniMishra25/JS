//Create a function that give us the average of 3 numbers

function averageofNum(num1, num2, num3) {
  let avg = (num1 + num2 + num3) / 3;
  console.log(`Average of ${num1}, ${num2} and ${num3} is : ${avg}`);
}

averageofNum(5, 10, 25);
